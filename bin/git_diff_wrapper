#!/bin/sh

if [ -e /usr/local/bin/nvim ]; then
  nvim -d "$1" "$2"
elif [ -e /usr/local/bin/vimdiff ]; then
  /usr/local/bin/vimdiff -v -d "$1" "$2"
elif [ -e /usr/bin/vimdiff ]; then
  /usr/bin/vimdiff -v -d "$1" "$2"
fi

